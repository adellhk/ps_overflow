<html>
<head>
  <title>PAR Stack DBC Overflow!!!</title>
</head>

<body ng-app="ParOverflow">
  <div ng-controller='HomeCtrl'>
    <div ng-model='variable'>
      <h2>{{variable}}</h2>
    </div>
    <ul ng-repeat='question in questions'>
      <li>
        <p>{{question.title}}</p>
        <p>{{question.content}}</p>
      </li>
    </ul>
  </div>

<a ng-click='myfunc()'>hsfasfi</a>
</body>
<html>
